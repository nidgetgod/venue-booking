apiVersion: v1
kind: Service
metadata:
  name: {{ include "venue-booking.fullname" . }}
  labels:
    {{- include "venue-booking.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "venue-booking.selectorLabels" . | nindent 4 }}
